version: 2
models:
  - name: stg_flights__bookings_append
    columns:
      - name: book_ref
        description: Уникальный код бронирования (6 символов).
        data_type: character varying
      - name: book_date
        description: Дата и время оформления бронирования (timestamp with time zone).
        data_type: timestamp with time zone
      - name: total_amount
        description: Общая стоимость бронирования (numeric).
        data_type: numeric
    description: >-
      **Description**: This model selects and provides a subset of the data from the "flights_staging__bookings_append"
      table, including booking references, dates, and total amounts. The upstream documentation provides
      definitions for these fields.
